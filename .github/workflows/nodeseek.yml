name: NodeSeek Signin

on:
  schedule:
    # 00:00 Asia/Shanghai (16:00 UTC)
    - cron: '0 16 * * *'
  workflow_dispatch:
    inputs:
      enable_statistics:
        description: 'Enable 30-day statistics'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
      random_mode:
        description: 'Signin mode (false=chicken legs x5, true=random)'
        required: false
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
      base_url:
        description: 'NodeSeek base URL'
        required: false
        default: ''
        type: string

env:
  TZ: Asia/Shanghai

permissions:
  contents: read

jobs:
  signin:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        version: latest
        virtualenvs-create: true
        virtualenvs-in-project: true

    - name: Install dependencies
      run: poetry install --no-interaction

    - name: Set env
      run: |
        echo "ENABLE_STATISTICS=${{ github.event.inputs.enable_statistics || 'true' }}" >> $GITHUB_ENV
        echo "NS_RANDOM=${{ github.event.inputs.random_mode || 'true' }}" >> $GITHUB_ENV
        echo "TIMEOUT=60" >> $GITHUB_ENV

    - name: Run signin
      env:
        NS_COOKIE: ${{ secrets.NS_COOKIE }}
        NS_COOKIE_WRITE_TOKEN: ${{ secrets.NS_COOKIE_WRITE_TOKEN }}
        GITHUB_REPOSITORY: ${{ github.repository }}
        COOKIE_STORE: ${{ vars.COOKIE_STORE || 'github' }}
        BASE_URL: ${{ github.event.inputs.base_url || vars.BASE_URL }}
        PROXY_URL: ${{ vars.PROXY_URL }}
      run: poetry run python -m nodeseek_signin

    - name: Upload logs
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: nodeseek-logs-${{ github.run_number }}
        path: |
          *.log
        retention-days: 7
